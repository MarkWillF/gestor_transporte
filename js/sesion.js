document.getElementById("iniciar_sesion").addEventListener("submit", function (event) {
    // Puedes agregar aquí cualquier lógica adicional antes del envío del formulario, si es necesario
    // ...

    // El formulario se enviará normalmente después de ejecutar esta función, a menos que se evite el envío
});